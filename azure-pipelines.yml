trigger:
  - main

variables:
  azureWebAppName: 'visionly'

stages:
  - stage: Build_and_Deploy_Backend
    jobs:
      - job: Deploy_Azure
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - task: NodeTool@0
            inputs:
              versionSpec: '20.x' # Kita samakan dengan versi di Azure Portal kamu

          # Gunakan Yarn untuk install
          - script: |
              yarn install
              yarn build --if-present
            displayName: 'Install and Build Backend with Yarn'

          - task: AzureWebApp@1
            inputs:
              azureSubscription: 'MyAzureConnection'
              appName: $(azureWebAppName)
              package: '$(System.DefaultWorkingDirectory)'

  - stage: Build_Android
    dependsOn: Build_and_Deploy_Backend
    jobs:
      - job: Build_APK_AAB
        pool:
          vmImage: 'ubuntu-latest'
        variables:
          - group: visionly-secrets
        steps:
          - task: NodeTool@0
            inputs:
              versionSpec: '20.x'

          # Gunakan Yarn di sini juga
          - script: yarn install
            displayName: 'Install Dependencies with Yarn'

          - script: |
              echo "GEMINI_API_KEY=$(GEMINI_API_KEY)" > .env
            displayName: 'Inject Gemini API Key'

          - script: |
              cd android
              chmod +x gradlew
              ./gradlew bundleRelease assembleRelease
            displayName: 'Build Android (AAB & APK)'

          - task: PublishBuildArtifacts@1
            inputs:
              PathtoPublish: 'android/app/build/outputs/bundle/release'
              ArtifactName: 'Android-App-Bundle'
              publishLocation: 'Container'

          - task: PublishBuildArtifacts@1
            inputs:
              PathtoPublish: 'android/app/build/outputs/apk/release'
              ArtifactName: 'Android-APK-Installable'
              publishLocation: 'Container'
